# resort-portfolio-demo

Create a separate static React + TypeScript frontend demo for the existing PHP + MySQL resort reservation system. This demo is for portfolio live preview on Vercel only and must not connect to or modify the real MySQL database. The front-end should be exactly the same from the existing project.
